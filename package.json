{
  "name": "places-i-shot",
  "private": true,
  "scripts": {
    "check:front": "node --check app.js",
    "dev:api": "npm --prefix server run dev",
    "start:api": "npm --prefix server run start"
  },
  "devDependencies": {
    "pm2": "^6.0.14"
  }
}
